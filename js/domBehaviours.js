// some dom behaviours
jQuery(function () {
    we.getHelper('dom').applyGlobalBehaviour('modals', {});
    we.getHelper('dom').applyGlobalBehaviour('activator', {});
    we.getHelper('dom').applyGlobalBehaviour('deactivator', {});
    we.getHelper('dom').applyGlobalBehaviour('modaleIframes', {});
});